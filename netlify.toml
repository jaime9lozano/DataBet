[build]
  base = "web"
  command = "npm ci --no-optional && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--loglevel=error"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
