[build]
  base = "web"
  command = "npm ci --omit=optional && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--loglevel=error"
  NPM_CONFIG_OPTIONAL = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
